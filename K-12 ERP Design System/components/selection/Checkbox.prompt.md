**Checkbox** — independent boolean or row selection. Use `indeterminate` for a "select all" that's partially checked.

```jsx
<Checkbox label="Send SMS to parents" defaultChecked />
<Checkbox indeterminate aria-label="Select all rows" />
```
