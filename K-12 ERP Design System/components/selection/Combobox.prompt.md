**Combobox** — searchable select for large or remote sets; `multi` renders removable chips.

```jsx
<Combobox label="Assign subjects" multi options={subjects} value={sel} onChange={setSel} />
```

Use over `Select` when the list is long, searchable, or multi-select.
