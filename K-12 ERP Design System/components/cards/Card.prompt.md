**Card** — the base surface for grouping content; add `title`/`actions`/`footer` slots.

```jsx
<Card title="Recent admissions" actions={<Button variant="link">View all</Button>}>
  …content…
</Card>
```

Variants: `default · interactive · selectable · outline`. Set `padded={false}` for flush tables/media.
