**SectionPanel** — titles and bounds a page section; set `collapsible` for progressive disclosure.

```jsx
<SectionPanel title="Guardian details" description="Primary contact for the student"
  actions={<Button variant="tertiary" size="sm" iconStart="pencil">Edit</Button>}>
  <DescriptionList items={…} />
</SectionPanel>
```
