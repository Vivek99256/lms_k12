**TopbarNav** — global app bar with brand, search, and account slots.

```jsx
<TopbarNav
  brand={<Brand />}
  search={<SearchInput placeholder="Search students, fees, staff…" />}
  actions={<IconButton icon="bell" label="Notifications" variant="ghost" />}
  account={<Avatar name="Admin" />}
/>
```
