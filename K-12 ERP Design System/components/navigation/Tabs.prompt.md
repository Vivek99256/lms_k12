**Tabs** — switch between peer sections without navigating.

```jsx
<Tabs activeId={tab} onChange={setTab} tabs={[
  { id: "overview", label: "Overview" },
  { id: "fees", label: "Fees", badge: 2 },
  { id: "attendance", label: "Attendance" },
]} />
```

Variants: `underline · enclosed`.
