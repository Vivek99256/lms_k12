**AuthForm** — login / forgot / reset shell in a centered card.

```jsx
<AuthForm mode="login" brand={<Brand />} onSubmit={signIn}
  footer={<>New institution? <a href="#">Request access</a></>} />
```

Modes: `login · forgot-password · reset-password`.
