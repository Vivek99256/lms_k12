**Tag** — category label or filter chip.

```jsx
<Tag variant="brand">Grade 9</Tag>
<Tag removable onRemove={() => drop("science")}>Science</Tag>
<Tag selected={active} onClick={toggle}>Boarding</Tag>
```
