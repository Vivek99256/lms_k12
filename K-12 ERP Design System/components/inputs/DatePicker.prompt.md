**DatePicker** — text field + month-calendar popover for selecting a date (value is a JS `Date`).

```jsx
<DatePicker label="Date of birth" value={dob} onChange={setDob} />
```

Sizes: `sm · md`. Pass `errorText` for the invalid state.
