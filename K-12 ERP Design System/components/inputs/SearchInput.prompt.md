**SearchInput** — query field for toolbars and global search; shows a clear button when filled and a `loading` spinner during async fetches.

```jsx
<SearchInput placeholder="Search students…" onChange={e => setQuery(e.target.value)} />
```
