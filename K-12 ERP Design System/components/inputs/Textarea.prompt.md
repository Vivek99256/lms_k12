**Textarea** — multi-line free text for notes, remarks and messages. Set `maxLength` (or `showCount`) to display a live character counter.

```jsx
<Textarea label="Remarks" rows={4} maxLength={280} placeholder="Add a note…" />
```
