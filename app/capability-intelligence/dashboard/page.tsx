'use client'

/**
 * Ported from G2G's `components/domain/competency/cm-command-center.tsx`
 * (mounted there directly; this route is new in this project's routing,
 * mirroring `talent-management/certifications/page.tsx`).
 */

import { CommandCenter } from './components/command-center'

export default function CapabilityIntelligenceDashboardPage() {
  return <CommandCenter />
}
