'use client'

/**
 * Ported from G2G's `components/domain/competency/cm-framework-mapping.tsx`
 * (mounted there directly; this route is new in this project's routing).
 */

import { FrameworkMapping } from './components/framework-mapping'

export default function CompetencyFrameworkPage() {
  return <FrameworkMapping />
}
