'use client'

/**
 * Ported from G2G's `components/domain/competency/cm-libraries-taxonomy.tsx`
 * (mounted there directly; this route is new in this project's routing).
 */

import { CapabilityLibraryTaxonomy } from './components'

export default function CapabilityLibraryPage() {
  return <CapabilityLibraryTaxonomy />
}
