'use client'

/**
 * Ported from G2G's `components/domain/competency/cm-competency-library.tsx`
 * (mounted there directly; this route is new in this project's routing,
 * mirroring `talent-management/certifications/page.tsx`).
 */

import { CompetencyLibrary } from './components/competency-library'

export default function CompetencyLibraryPage() {
  return <CompetencyLibrary />
}
