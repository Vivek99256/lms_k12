'use client'

/**
 * Route for the Organization Management "Compliance Library" screen.
 * Ported as-is from G2G's
 * `components/domain/hrms/compliance-discipline/compliance-library-management.tsx`
 * (see that component's header comment for the data-layer adaptation notes).
 */

import { ComplianceLibraryManagement } from './components'

export default function ComplianceLibraryPage() {
  return <ComplianceLibraryManagement />
}
