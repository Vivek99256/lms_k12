"use client";

/**
 * The chained grade/standard/division filters are shared with the Admin
 * services menus, so they now live in `@/components/erp/ClassFilters`.
 */
export { ClassFilters, emptyClassSelection } from "@/components/erp/ClassFilters";
export type { ClassSelection } from "@/components/erp/ClassFilters";
