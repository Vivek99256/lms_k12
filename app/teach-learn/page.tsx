'use client';

import LmsDashboardPage from '@/app/lms/dashboard/page';

/** Teach/Learn opens on the existing LMS Dashboard inside the module shell. */
export default function TeachLearnPage() {
  return <LmsDashboardPage />;
}
