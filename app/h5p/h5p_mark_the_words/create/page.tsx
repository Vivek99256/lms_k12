'use client';

import { TextActivityCreatePage } from '../../text_activity/components/screens';

/** MarkTheWords — create. Shared screen; see screens.tsx. */
export default function Page() {
  return <TextActivityCreatePage type="mark_the_words" />;
}
