'use client';

import { TextActivityViewPage } from '../../text_activity/components/screens';

/** MarkTheWords — play / preview. Shared screen; see screens.tsx. */
export default function Page() {
  return <TextActivityViewPage type="mark_the_words" />;
}
