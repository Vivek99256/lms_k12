'use client';

import { TextActivityEditPage } from '../../../text_activity/components/screens';

/** MarkTheWords — edit. Shared screen; see screens.tsx. */
export default function Page() {
  return <TextActivityEditPage type="mark_the_words" />;
}
