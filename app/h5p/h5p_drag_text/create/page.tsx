'use client';

import { TextActivityCreatePage } from '../../text_activity/components/screens';

/** DragText — create. Shared screen; see screens.tsx. */
export default function Page() {
  return <TextActivityCreatePage type="drag_text" />;
}
