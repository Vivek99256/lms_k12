'use client';

import { TextActivityEditPage } from '../../../text_activity/components/screens';

/** DragText — edit. Shared screen; see screens.tsx. */
export default function Page() {
  return <TextActivityEditPage type="drag_text" />;
}
