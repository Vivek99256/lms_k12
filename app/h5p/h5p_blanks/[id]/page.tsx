'use client';

import { TextActivityViewPage } from '../../text_activity/components/screens';

/** Blanks — play / preview. Shared screen; see screens.tsx. */
export default function Page() {
  return <TextActivityViewPage type="fill_in_the_blanks" />;
}
