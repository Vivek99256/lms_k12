'use client'

/**
 * Route for Task Management > Administration > Permissions Matrix.
 * Ported from G2G's `tm-permissions.tsx`. See `PermissionsMatrixCenter` for
 * the full port notes.
 */

import { PermissionsMatrixCenter } from './components'

export default function PermissionsMatrixPage() {
  return <PermissionsMatrixCenter />
}
