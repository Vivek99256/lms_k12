'use client'

/**
 * Route for Task Management > Administration > Audit Logs.
 * Ported from G2G's `tm-audit-logs.tsx`. See `AuditLogsCenter` for the full
 * port notes.
 */

import { AuditLogsCenter } from './components'

export default function AuditLogsPage() {
  return <AuditLogsCenter />
}
