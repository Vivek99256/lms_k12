'use client'

/**
 * Route for Task Management > Administration > Status Management.
 * Ported from G2G's `tm-status-management.tsx` + `tm-priority-management.tsx`
 * (see `StatusManagementCenter` for the combined-page rationale).
 */

import { StatusManagementCenter } from './components'

export default function StatusManagementPage() {
  return <StatusManagementCenter />
}
