'use client'

/**
 * Route for Task Management > Administration > Integration.
 * Ported from G2G's `tm-integrations.tsx`. See `IntegrationCenter` for the
 * full port notes.
 */

import { IntegrationCenter } from './components'

export default function IntegrationPage() {
  return <IntegrationCenter />
}
